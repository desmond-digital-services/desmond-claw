# Desmond Agent Platform - Docker Environment Variables
# Copy to .env and update values

# Required: Gateway token (generate with: openclaw gateway token)
OPENCLAW_GATEWAY_TOKEN=your_gateway_token_here

# Domain for production SSL
DOMAIN=yourdomain.com

# Optional: Default channels (comma-separated)
# OPENCLAW_DEFAULT_CHANNELS=telegram,discord,whatsapp

# Optional: Business mode
# OPENCLAW_BUSINESS_MODE=true
